{
  "name": "swagger",
  "version": "1.0.0",
  "description": "swagger",
  "scripts": {
    "merge": "swagger-merger --debug -i ./src/index.yml -o ./src/swagger.yml",
    "watch": "gulp"
  },
  "dependencies": {
    "gulp": "^4.0.2"
  }
}
